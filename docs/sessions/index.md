---
title: Development log
---

# Development log

One archive per working session. Each summary records what shipped, the
decisions and their reasoning, and — the load-bearing part — the dead ends:
what was tried, what failed, and why. They are written for whoever picks the
work up next, which is usually a fresh agent with no memory of the session.

Each session's planning files (`task_plan.md`, `findings.md`, `progress.md`)
are archived beside its summary as `planning.tar.gz` in the repository.

| Date | Session |
|---|---|
| [2026-09-23](2026-09-23-codex-support/summary.html) | Codex beside Claude with ONE active provider per worktree. The side-by-side plan was drafted and dropped, because two agents writing one tree is a conflict generator. `new`/`open --ai` close the other Worktrees-managed session first and start nothing if that fails, and a git-dir lock serialises app, CLI and MCP opens. A session adopted from a personal tmux name is refused by name, never killed. Each provider keeps its own saved conversation, so a switch loses nothing: Codex resumes with `resume --last` only when a rollout's `session_meta` names this exact cwd. MCP setup goes through `codex mcp add\|remove` and never edits Codex's config, and sign-in is ChatGPT OAuth only, with no API keys. Switch moved into the place menu behind a confirmation. The recorded gotcha is the sandbox registering `~/.local/bin/worktrees` instead of the branch build, now `WORKTREES_CLI_BIN`. Archived without planning files; the CHANGELOG entry and a live-switch manual checklist are still owed |
| [2026-09-22](2026-09-22-project-status-pane/summary.html) | a Plan tab in the dock that reads the session's planning-with-files plan and never writes it — the design question was where (a fourth dock tab, not a band or a second pane), what session integration (the live state chip and unsent draft the app already had, plus MCP `place_status` carrying the same summary), and structured-vs-markdown, which a ten-minute survey settled: of fifteen real `task_plan.md` files, two used the template's `### Phase` + `**Status:**` markers, so a parser written to the template would have shown nothing on thirteen places and passed every test; the extractor mirrors the skill's own Stop-hook greps instead and degrades to the rendered file. Resolution mirrors `resolve-plan-dir.sh` (`.active_plan`, newest plan dir, root file, brief only), lstat per component with `O_NOFOLLOW`. Six header directions were drawn on a design canvas and David chose brief-first with a single dense band; the empty state then stopped being blank (branch, divergence, dirty, last commit, last worked, note, Claude chip) and gained "Generate plan", which PASTES a fixed prompt into the place's Claude pane through the drag-and-drop path — no newline, no directory named, because the skill may write at the repo root — so the session, not the app, writes the plan. Two Opus agents built the halves in parallel against a pinned contract; the gotchas were a `::before` separator dangling on an ellipsised span, a header `max-height` that made two nested scrollers, and the age orphaning at 240px depending on its position in the band |
| [2026-09-21](2026-09-21-planning-docs-index/summary.html) | the plan a session works from was in the one directory the Docs index could not enter — `.planning/brief.md` was listed by NAME and everything beside it was unreachable, because every tree walk refuses a dotted name on the way DOWN (which is what keeps a nested `.git` out), so valleos showed 1 of 9 planning documents on one place and 1 of 19 on another; it was never intermittent, it never worked. Step 4 of the walk now lists the whole directory, grouped by workstream, and neither frontend needed a line — both already nest on the backend's `group` and both path validators already allowed a leading dot, which is the one-function seam the proposal was cut for. Review found the three things the author could not: a test that passed because its fixture omitted a `brief.md` behind the symlink it was guarding (`symlink_metadata` refuses to follow only the LAST component, so the by-name read went through a link the walker beside it correctly refused — a partial guard looks exactly like a working one), five confident repetitions of a sentence that was simply false (`paths = [".planning"]` had always worked), and a case-variant declaration that listed everything twice on APFS. The general symlink hole is recorded rather than patched, because a `docs -> ../shared` is a reasonable thing for a repo to do and the server already 403s the rows it produces |
| [2026-09-20](2026-09-20-symlink-refusal-remedy/summary.html) | a red refusal in the middle of a worktree that had in fact succeeded — check B3 correctly refuses a declared file whose source is a symlink out of the repo (the hostile `.env -> ~/.ssh/id_rsa` case), but told you to "link the real file, or point the config at it" and BOTH are impossible in the only case that reaches it: `path` is repo-relative and the target is outside the repo by definition. A sibling project's `_tmp -> iCloud` entry had failed on all eight places for six days with no readable way forward, disguised by four worktrees somebody had symlinked by hand — a feature manually patched where it fails looks like a feature that works. The rule and its exit 2 are unchanged; the message now says the worktree is otherwise complete and names the way out, which is to COMMIT the link rather than declare it, because `git worktree add` checks out a tracked 120000 blob with no Layer B check at all. That advice was driven end-to-end through the real binary rather than asserted as a string, since the advice was the defect. Downstream fix delegated to the other project's own session and re-verified here; review then found the one thing nobody had: a tracked symlink is branch-dependent, so checking out a branch that predates it DELETES the link until rebase, idle bases included |
| [2026-09-20](2026-09-20-terminal-history/summary.html) | a dock shell tab keeps what it was showing and what it ran — the tab already remembered its name, its slot and its directory, then opened blank, because the PTY is ours and dies with the app; now its last 256K replays under a dim restored rule and each tab gets its own HISTFILE, which cannot be `HISTFILE` at all because macOS's `/etc/zshrc:16` overwrites it unconditionally from `${ZDOTDIR:-$HOME}`, so the lever is ZDOTDIR and the generated shims have to hand it BACK twice (a `~/.zshenv` that exports it would hijack the chain; a `.zshrc` that reads `$ZDOTDIR` must find its own files). Two latent bugs surfaced on the way: xterm sat at its default 1000 lines while the backend replayed 3200, dropping the oldest two thirds of every replay, and a recording laid out at another width stacked a fragment of every full line. Three of the session's own mistakes are the useful part — a partial-line trim that COMPOUNDED one lost line per restart, a seam kept out of the ring and therefore invisible past the first tab flip, and a phantom-tab "bug" that was the test harness clicking + itself, caught only by making the repro name its caller. Plus a reported bug that pre-dated all of it: named tabs vanishing on restart because the restore read settings that had not landed, fixed with the `hydratedTick` that #302 had already added for exactly this and given exactly one consumer |
| [2026-09-20](2026-09-20-live-docs-viewer/summary.html) | a place's documents, in the real browser, live as you edit them — the third-party viewer the whole design rested on turned out never to validate the `Host` header (GHSA-6pff-wf7m-6f5h), and a proxy cannot fix a server that holds its own loopback port, so the server is ours: conditional GET rather than SSE (six connections per origin, and the sixth open document wedges the page), block-level patching keyed on a hash of each block's markdown so an edit lands without moving your scroll or killing your selection, and mermaid admitted as the second dependency exception ONLY because the viewer is a separate lib-mode build that a shared chunk cannot hoist into `app/src`. Seven defects in the first integration were found by running the app and none by a gate; a post-merge review then found three more, including a back-navigation that hung on "loading …" forever because the ETag was cached and the payload was not, and a removed place handing its route segment — same token, same port, same URL — to the next project with that slug. Two lessons cost the most: a bind assertion "measured on this platform" was measured on macOS and is false on Linux in the opposite direction, and a workflow that fails to PARSE is invisible to every PR check |
| [2026-09-20](2026-09-20-unread-ring-restart/summary.html) | the unread ring came back on every restart — the ack saved fine, the other side of the comparison moved: the startup backfill dated a session by its transcript's MTIME, and Claude Code rewrites a live session's `.jsonl` for hours after its last turn (24 of 28 transcripts measured 12 minutes to 34 HOURS ahead of their newest entry), so every launch re-dated every still-open place to "just finished" — past the seen stamp, and past the row age and sort key the same field feeds; now dated by the timestamps inside the file. Plus the ack's own gate: it guarded on the DISPLAY predicate, so a visit to a place whose session sits at `waiting` — the state it is in because it wants you — spent nothing and the ring returned when that session went quiet. Four wrong hypotheses first; what found it was five places whose stamp equalled a file mtime exactly |
| [2026-09-19](2026-09-19-usage-meter-degrade/summary.html) | the usage meter hid instead of ageing — a screenshot asking "why did I temporarily not see it" was a 4m45s HTTP 429 on the OAuth usage endpoint, during which a reading 130 seconds old (indistinguishable from a fresh one at the resolution of a 4px bar) was thrown away to show nothing at all; worse, the 120s floor between real fetches had only ever applied after a SUCCESS, so nothing was cached on failure and every window-focus pull went straight back out — 15 real requests inside one minute, a rate limit held open by the thing complaining about it. A failed poll now serves the last live reading, dimmed and dated, bounded at 30 minutes; a failure buys the same 60s of quiet a success gets. Two things the session turned up: the sibling `claude_status` had already made exactly this bargain 300 lines down the same file and nobody had noticed (found by a test-name collision), and review caught the degrade reading a PRE-fetch snapshot of the cache, so under the doubled pull it was fixing, the loser of the race would dim a live widget for a whole poll period |
| [2026-09-19](2026-09-19-mcp-offer-surfaces/summary.html) | the suggestion nobody saw — v0.25.0's offer to wire up the MCP server was correct about the machine and unreachable in practice: it rendered only on Home AND only with a project, two surface preconditions that each read as reasonable and multiply to almost never, while the release notes — which have none — already carried the same sentence as prose with nothing to press; replaced by an offer registry whose action is a DEEP LINK rather than the deed (the panel's Set up carries a permissions checkbox, so an inline button would decide it for you, and N offers stay N rows instead of N embedded panels), a band pinned above the notes because three separate attempts had put the thing to DO after the thing to READ, and a purple gear dot — which review caught having no off switch at all on a fresh install, the old Home card's one-way door rebuilt from the other direction |
| [2026-09-18](2026-09-18-drag-pane-fix/summary.html) | the drag never found Claude — v0.25.0 shipped it and it could not work anywhere: the AI pane was matched on the process name, but a natively-installed claude is a symlink to `versions/2.1.277` and macOS reports the RESOLVED name, so the match found the pane in 0 of 21 live sessions; fixed in v0.25.1 by requiring launched-as-AI AND still-running-AI (the start command alone matches a pane whose Claude has EXITED, which is how the first fix re-created the bug), with no sole-pane fallback because the app makes single-pane sessions and that is equally the shape of a dead one
| [2026-09-18](2026-09-18-drag-reference/summary.html) | drag a worktree into a session to talk about it — the other half of the reference ask: dropping a place row on the terminal types the same `@worktrees:place://…` token the `@` menu completes, built in Rust so the two producers cannot drift; and two things that were quietly wrong — a tmux NAME target PREFIX-matches, so `-t api:0.0` pasted into `api-fix`, and "pane 0 is the AI" fails under `base-index 1`, after any pane renumber, and in every adopted session, so the pane is found by what it RUNS and addressed by a `PaneId` newtype the wrong target cannot even be spelled as
| [2026-09-18](2026-09-18-mcp-setup-wizard/summary.html) | the app wires Claude's MCP server up for you — a Settings → Claude panel and a dismissible Home card that register `worktrees mcp` at user scope, via `claude mcp add` and NEVER by editing `~/.claude.json` (claude rewrites that file whole from every live session); detection parses the file instead of asking `claude mcp list`, which health-checks by LAUNCHING each server in the cwd and so calls one correct install `✘ CONNECTION_CLOSED` from /tmp and `✔ Connected` from a checkout; four states rather than two, so a stanza whose binary moved offers Repair and a dismissal of the install offer never silences the broken-server warning — and the server itself had to learn to serve with no project, because a user-scope install is launched by every session including the ones outside a repo |
| [2026-09-18](2026-09-18-mcp-resources/summary.html) | reference another worktree while you type — `worktrees mcp` publishes every place as an MCP resource, so `@worktrees:place://bug-fixes` completes from claude's own `@` menu and expands into that place's branch, divergence, dirty state and tmux session; the picker stays current because the server PUSHES `resources/list_changed` off a membership-only signal (no periodic ping exists to piggyback on), and the uris are sanitised narrower than BOTH of the client's disagreeing mention rules — the menu charset admits `(` and `)` that the submit regex's trailing `\b` then rejects, so `place://(main)` completed and resolved to nothing
| [2026-09-18](2026-09-18-claude-status-indicator/summary.html) | the app says so when Claude itself is down — a poll of status.claude.com surfaced only when something is wrong, scoped to the two components a worktree can feel (`Claude Code`, `Claude API`) and never to the page's own six-component verdict; the indicator follows `usage_place` (chip in the strip/footer, corner dot on the rail tile, nothing on `off`), and the severity tokens turned out to fail 4.5:1 as text in four of six themes, so only dots and tints carry hue |
| [2026-09-16](2026-09-16-dim-suggestion/summary.html) | ✎ ignores Claude's dimmed suggestion — session summary |
| [2026-09-16](2026-09-16-unread-afterglow/summary.html) | unread completions — the afterglow dot grows to 10px inside a second ring and holds tier 1 until the place is SEEN (selected in a visible window for a 1s dwell, or entered), then drops to its real age; `Declared.last_seen_epoch` via a forward-only `mark_seen`, the rule as three pure functions in `afterglow.ts` under the fail-first check; bold row names rejected |
| [2026-09-16](2026-09-16-remote-links/summary.html) | the repo on its remote, one click away — right-click a project for its GitHub home, and a link beside the topbar's branch chip that opens the branch's own page when it is on origin and the repo home when it is not (the old place-menu link built `/tree/<local branch>` and 404'd on every unpushed branch); the page rule moved out of Rust into a pure `remote.ts` with a fail-first check script, and the backend only normalises the remote spec |
| [2026-09-11](2026-09-11-shell-replay-mute/summary.html) | a dock shell typed `2RR0;276;0c11;rgb:…` onto its own prompt after every place switch — terminal REPLIES, not garbage: the tab replays its recorded output into a fresh xterm on re-attach, and vim's startup query burst (left by every `git commit` without `-m`) was re-asked and re-answered down the pty; the pane now mutes replies while the recording parses, keyed on channel position because a Tauri channel payload that size arrives by a separate fetch that can land after the invoke resolves |
| [2026-09-09](2026-09-09-afterglow-decay-setting/summary.html) | the afterglow's horizon and step count become Settings → Navigation → Afterglow — a stop table from 1h to 7d and 2–6 tiers, spaced geometrically from a first boundary pinned at 15 minutes so the "go look" tier and the folder rollup never stretch with the tail; the math moves to a pure `afterglow.ts` with a fail-first check script, and opacity becomes an inline style because a stylesheet cannot know the step count |
| [2026-09-07](2026-09-07-esc-modals/summary.html) | Escape dismisses every dialog even with the terminal focused — xterm stops propagation of each keydown it handles and a WKWebView click never moves focus, so a dialog opened from a menu never heard its own Escape and the ESC went to tmux; one capture-phase stack (`useEscape`) now owns the key for every modal surface, ordered by activation so stacked surfaces close one at a time |
| [2026-09-07](2026-09-07-usage-meter-home/summary.html) | the Claude usage bars get a home you choose — a window-wide strip, the terminal footer, or a rail tile, behind Settings → Appearance — the branch chip becomes the switcher and the footer's tmux facts go; and a bug the Chrome harness cannot see: WebKit shrink-wraps a `<button>` flex container without its hidden-overflow children and counts nothing for a span sized only by `flex-basis`, reproduced and fixed in headless WebKit |
| [2026-09-05](2026-09-05-ui-overhaul/summary.html) | UI overhaul — one sidebar control (auto-hide overlay), Settings modal + compact notes + chord guard, local usage metrics, unsent-prompt ✎ |
| [2026-09-02](2026-09-02-agent-brief/summary.html) | agents — brief, `--name`, agent state in MCP; strays in `ls`/doctor |
| [2026-08-30](2026-08-30-status-inline/summary.html) | a worktree with no session now opens onto its own status check instead of one line and a button, and Claude's read is rendered rather than printed — one `StatusBody` shared by both hosts so they cannot drift; the menu item steps aside only when the check is already on screen for *that* place, because right-click does not select; and four defects that every unit gate passed — a recessed box with zero contrast against the surface it sits on (invisible outright in tokyo-day), a shared class quietly restyling the dock's empty states, 32px of padding lost to `align-items: stretch`, and a code fence sized in absolute units painting bigger than the prose around it |
| [2026-08-28](2026-08-28-sync-doctor-heal/summary.html) | eight of ten worktrees were silently carrying walls of `deleted:` tarballs — the #146 damage class, invisible between syncs because the heal was pull-only; 172 files healed by hand, then `doctor` learned to name the damage (suppressed in hub copies, where absence is the pushed state and the remedy is refused), the heal moved to every sync edge including a pull whose transfer failed, and `.worktrees-sync/` joined the `info/exclude` entries |
| [2026-08-29](2026-08-29-writing-tools-crash/summary.html) | the app aborted whenever you selected text — macOS 26's Writing Tools affordance trips an assertion inside AppKit, and that NSException unwinds through tao's `extern "C"` `sendEvent:`, so Rust turns a foreign unwind into SIGABRT; the tell is `panic in a function that cannot unwind` with **no** panic line before it, and the diagnosis lives in the unified log, not the crash report |
| [2026-08-29](2026-08-29-status-check/summary.html) | clicking a worktree stopped spawning sessions (the root cause of "everything reads active"), the header stopped saying things twice, ↓ became main-only, stale rows dim, and right-click grew a status check — a verdict computed once in core, with an Ask Claude button that is the repo's first headless spawn; four stacked PRs, an adversarial spec pass that caught a guard built on an invented fact, and a merge train whose only conflicts were three parallel `[Unreleased]` sections |
| [2026-08-29](2026-08-29-app-zoom/summary.html) | ⌘+/⌘− became an overall-size knob that finally reaches the terminal and its tmux pane — WKWebView page zoom, because `--ui-rem` deliberately never touched the xterm grid; and ⌥ chords cannot be matched on `e.key`, since macOS composes ⌥- into an en dash |
| [2026-08-18](2026-08-18-remove-dialog-menu-clamp/summary.html) | removing a worktree became a dialog that says what it will destroy, and a menu near the bottom edge stopped hiding its last item — a clamp keyed on frozen cursor coords, and `--force` turning out to mean `git branch -D` as well |
| [2026-08-18](2026-08-18-side-by-side-diff/summary.html) | the Files tab shows what changed, not just what is there — git's full-context diff in two aligned columns, word-level marks, and a changed-files-only tree; `git -C ""` means *here*, and a sticky cell tinted over `transparent` is see-through |
| [2026-08-18](2026-08-18-new-worktree-dialog/summary.html) | new worktree became a dialog that says what it will do — the branch picker gained a ▾ and got shared, and the verdict line had to learn `cmd_new`'s ordering, which is not the obvious one |
| [2026-08-18](2026-08-18-terminal-dock-overlap/summary.html) | the terminal ran on under the Files dock — a row flex item with no `min-width: 0` is floored at xterm's painted grid, so the pane could grow and never shrink, and the resize observer watched the box that could not move |
| [2026-08-17](2026-08-17-gitignore-cmdt-replay/summary.html) | new projects start with a clean `git status`, ⌘T + a files right-click menu, and the shell tab that came back four prompts deep — the ring replays fine at its own width; one column off is what stacks it |
| [2026-08-17](2026-08-17-parked-busy-dot/summary.html) | a parked job pinned the green dot on — the probe file keeps a mid-flight `busy` when a turn is parked, and nothing ever writes it again |
| [2026-08-17](2026-08-17-emoji-width/summary.html) | emoji stopped shredding tmux output — tmux said 2 cells, xterm said 1; the graphemes addon aligns them, and the Node width probe lies |
| [2026-08-16](2026-08-16-sync-macs/summary.html) | `worktrees sync` — courier sync between Macs on an SSD, plan to six merged PRs: CLI parity, guards on every surface, the app's modal + progress bar + import, and the heal for tracked files the transfer skips |
| [2026-08-14](2026-08-14-drag-drop-nav/summary.html) | drag a place into another group to put it there — a drop sets the tier, the gap opens where it will really land, projects reorder too |
| [2026-08-14](2026-08-14-cmd-f-find/summary.html) | ⌘F finds — the xterm buffer in either terminal, the open file in the viewer, one bar routed by what you last touched |
| [2026-08-14](2026-08-14-markdown-zoom/summary.html) | markdown docs read at whatever size you need — a per-place reading zoom for the Files viewer, one multiplier and a rem→em sweep |
| [2026-08-14](2026-08-14-terminal-tab-memory/summary.html) | terminals come back as you left them — the tab strip, the front tab and each tab's directory all survive a restart → v0.13.0 |
| [2026-08-13](2026-08-13-codesign-privacy-prompts/summary.html) | macOS privacy prompts and what signing them would cost — ad-hoc signing keys TCC to a cdhash; MAS ruled out, Developer ID scoped |
| [2026-08-11](2026-08-11-cmdk-activity-order/summary.html) | one clock for every list of places — ⌘K, Recent and Resume ranked three different ways, now all on the nav's activity date |
| [2026-08-11](2026-08-11-v0-12-releases/summary.html) | the sandbox found what the mock harness could not — dock default, nav refresh race → v0.12.0, v0.12.1 |
| [2026-08-11](2026-08-11-files-changed-markers/summary.html) | the Files tab says what the branch changed — tinted names, cascading counts, ghost rows for deletions |
| [2026-08-11](2026-08-11-space-workbench/summary.html) | a space owns its whole workbench — header over terminal+dock, per-place panel memory, nameable places |
| [2026-08-10](2026-08-10-files-tab-visibility/summary.html) | the Files tab stops hiding things silently — gitignored by default, symlinks marked → v0.11.0 |
| [2026-08-10](2026-08-10-global-skills-repo/summary.html) | close-out leaves the repo — a personal skills repo at `~/workspace/claude-skills` |
| [2026-08-10](2026-08-10-nav-activity-age/summary.html) | nav clock + order track activity, not attention → v0.11.0 |
| [2026-08-09](2026-08-09-afterglow-dot/summary.html) | the afterglow dot — a place stays lit after Claude finishes → v0.10.0 |
| [2026-08-09](2026-08-09-files-tab-refresh/summary.html) | the Files tab shows files as they are created → v0.10.0 |
| [2026-08-07](2026-08-07-power-consumption/summary.html) | the app stopped burning power in the background → v0.9.1 |
| [2026-08-07](2026-08-07-relnotes-inline-markdown/summary.html) | release notes render their own markdown |
| [2026-08-07](2026-08-07-new-place-feedback/summary.html) | creating a place says so, reopening one stops shouting |
| [2026-08-06](2026-08-06-nav-hierarchy/summary.html) | nav hierarchy — projects up, dormant down |
| [2026-08-06](2026-08-06-empty-project-onboarding/summary.html) | empty-project onboarding — git init + first commit |
| [2026-08-06](2026-08-06-ai-rules-layer/summary.html) | AI profiles — per-project Claude rules, skills, MCP and model |
| [2026-08-06](2026-08-06-usage-countdown/summary.html) | time-until-reset on the usage bars |
| [2026-08-05](2026-08-05-undeclared-drift/summary.html) | undeclared-file drift + ADR 0001 |
| [2026-08-03](2026-08-03-files-viewer/summary.html) | dock Files tab — document rendering + layout |
| [2026-08-02](2026-08-02-usage-widget/summary.html) | Claude plan-usage widget → v0.7.0 |
| [2026-08-02](2026-08-02-remove-place-delbranch/summary.html) | remove was broken for every place (`delBranch`) + stale "Not configured" banner |
| [2026-08-01](2026-08-01-ui-tweaks/summary.html) | UI batch — named terminal tabs, settings categories, guide-line toggle, log tail (+ v0.6.0 release) |
| [2026-08-01](2026-08-01-single-pane-new/summary.html) | app-created places open single-pane |
| [2026-07-30](2026-07-30-tmux-gate/summary.html) | tmux install gate + missing-tmux banner |
| [2026-07-29](2026-07-29-right-panel/summary.html) | right panel, icon set, owned dock shells, nav fixes → v0.5.0 |
| [2026-07-28](2026-07-28-right-dock/summary.html) | right dock — file browser, editable viewer, embedded terminals |
| [2026-07-28](2026-07-28-project-settings/summary.html) | Session — per-project settings (`.worktrees.toml`) |
| [2026-07-27](2026-07-27-settings-and-audits/summary.html) | settings evaluation, nav audits, ⌘K, v0.3.0 + v0.3.1 |
| [2026-07-27](2026-07-27-release-notes-ui/summary.html) | release-notes UI — 2026-07-27 |
| [2026-07-27](2026-07-27-readme-media/summary.html) | README desktop-app media — session summary |
| [2026-07-27](2026-07-27-close-out-ritual/summary.html) | close-out ritual + session archive system |
| [2026-07-27](2026-07-27-busy-dots-nav-home/summary.html) | busy-only dots, nav with Home, system theme pairs |
| [2026-07-26](2026-07-26-themes-v0.2.4/summary.html) | light mode + theme gallery → v0.2.4 |
